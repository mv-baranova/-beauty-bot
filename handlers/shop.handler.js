/**
 * Placeholder for WB search feature.
 */
async function wbSearchHandler(ctx) {
  await ctx.reply('🛍️ подбор с Wildberries — я найду для тебя лучшие айтемы по твоему запросу ✨ (уже в пути)');
}

module.exports = {
  wbSearchHandler,
};
